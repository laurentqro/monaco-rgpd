<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <title>Registre de Traitement</title>
  <style>
    /* General styling */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      font-size: 10pt;
      line-height: 1.4;
      color: #000;
      padding: 20px;
    }

    /* Header */
    .registre-header {
      background-color: #C5E0B4;
      padding: 15px;
      text-align: center;
      margin-bottom: 20px;
      border: 2px solid #000;
    }

    .registre-header h1 {
      font-size: 14pt;
      font-weight: bold;
    }

    /* Sections */
    section {
      margin-bottom: 20px;
      page-break-inside: avoid;
    }

    h2 {
      background-color: #C5E0B4;
      padding: 8px;
      font-size: 12pt;
      margin-bottom: 10px;
      border: 1px solid #000;
    }

    /* Tables */
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 10px;
    }

    thead {
      background-color: #C5E0B4;
    }

    th, td {
      border: 1px solid #000;
      padding: 8px;
      text-align: left;
      vertical-align: top;
    }

    th {
      font-weight: bold;
      font-size: 10pt;
    }

    td {
      font-size: 9pt;
    }

    tbody tr:nth-child(even) {
      background-color: #f9f9f9;
    }

    /* Metadata fields */
    .metadata {
      margin-bottom: 15px;
    }

    .field-row {
      display: flex;
      border: 1px solid #000;
      margin-bottom: -1px;
    }

    .field-label {
      background-color: #C5E0B4;
      padding: 8px;
      font-weight: bold;
      flex: 0 0 50%;
      border-right: 1px solid #000;
    }

    .field-value {
      padding: 8px;
      flex: 1;
    }

    .field-value ul {
      list-style-type: none;
      margin: 0;
      padding: 0;
    }

    .field-value li {
      margin-bottom: 3px;
    }

    /* Orange highlighting for special fields */
    .highlight-orange .field-label,
    .highlight-orange .field-value {
      background-color: #FFC000;
    }

    /* Page breaks */
    @media print {
      section {
        page-break-inside: avoid;
      }

      table {
        page-break-inside: auto;
      }

      tr {
        page-break-inside: avoid;
        page-break-after: auto;
      }
    }
  </style>
</head>
<body>
  <%= yield %>
</body>
</html>
